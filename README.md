# ROZVIK

Website + backend for the ROZVIK storefront (premium nuts, dry-fruit
ladoos and handmade chocolates).

Everything lives in `backend/` — see [`backend/README.md`](backend/README.md)
for how to run it. The website itself is `backend/public/index.html`,
served by the backend along with its API (products, orders, OTP login).
